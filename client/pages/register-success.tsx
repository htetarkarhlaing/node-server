import type { NextPage } from "next";
import { Main } from "../layouts";

const RegisterSuccess: NextPage = () => {
  return (
    <Main SEO={{ title: "Register" }}>
      <div>Hello</div>
    </Main>
  );
};

export default RegisterSuccess;
